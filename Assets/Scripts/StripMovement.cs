using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StripMovement : MonoBehaviour
{
    //GameObject[] stripes;
    //RigidBody StripeRB;

    //void Start()
    //{
    //    stripes = GameObject.FindGameObjectsWithTag("stripe");
    //}

    //void Update()
    //{
    //    MoveStripes();
    //}

    //void MoveStripes()
    //{

    //    //StripeRB = GetComponent<Rigidbody>();

    //    //for (int i = 0; i < stripes.Length; i++)
    //    //{
    //    //    GameObject stripe = stripes[i];

    //    //    if (stripe != null)
    //    //    {
    //    //        stripe.transform.position = new Vector3(stripe.transform.position.x, stripe.transform.position.y, stripe.transform.position.z - (Time.deltaTime * 10f));
    //    //        StripeRB.AddForce();

    //    //        if (stripe.transform.position.z <= 98)
    //    //        {
    //    //            stripe.transform.position = new Vector3(stripe.transform.position.x, stripe.transform.position.y, 432f);
    //    //        }
    //    //    }
    //    //}
    //}
}
